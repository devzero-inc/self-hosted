## @section Redis Database Configuration
## @descriptionStart
## 1. POLLAND/BUILDQD: 0
## 2. BACKEND: 1
## 3. HYDRA: 2
##
## @descriptionEnd

## @section Chart Configuration
## @param nameOverride String to override the chart's name
nameOverride: ""
## @param fullnameOverride String to override the chart's computed fullname
fullnameOverride: ""
## @param domain Domain name for the installation
domain: "example.com"
## @section Credentials Configuration
credentials:
  ## @param credentials.enable Enable docker hub credentials
  # Comment out image.pullSecrets if you disable this
  enable: true
  ## @param credentials.registry Container registry URL
  registry: "docker.io"
  ## @param credentials.username Registry username
  username: ""
  ## @param credentials.password Registry password
  password: ""
  ## @param credentials.email Registry email address
  email: ""

## @section TLS Configuration
tls:
  ## @param tls.enabled Enable TLS
  # Disable this to remove TLS configuration in all ingresses
  enabled: true

## @section Image Configuration
## @param image.repository Devzero container image repository
image:
  ## @param image.repository Devzero container image repository
  repository: docker.io/devzeroinc
  ## @param image.tag Devzero container image tag
  tag: "v1.0.10"
  ## @param image.pullPolicy Container pull policy
  pullPolicy: IfNotPresent
  # -- Optionally specify an array of imagePullSecrets
  # pullSecrets:
  #   - pull-secret
  ## @param image.pullSecrets Optionally specify an array of imagePullSecrets
  pullSecrets:
    # Comment this out if you are not using credentials.enabled=true
  - pull-secret

## @section Workspace Configuration
workspace:
  ## @param workspace.image Base image for workspaces
  image: public.ecr.aws/v1i4e1r2/devzero-devbox-base
  ## @param workspace.localTag Tag for local workspace image
  localTag: base-latest
  ## @param workspace.readOnlyHostPathVolumes Read-only host path volumes to mount inside of each workspace.
  ## @extra workspace.readOnlyHostPathVolumes[0].name The name for this read-only host path volume.
  ## @extra workspace.readOnlyHostPathVolumes[0].source The path on the host from which you'd like to create this volume.
  ## @extra workspace.readOnlyHostPathVolumes[0].mountPath The path on the workspace into which you'd like to mount this volume.
  readOnlyHostPathVolumes: []
  ## @section Base Image Configuration
  baseImages:
    ## @param workspace.baseImages.sysboxBase Base image for sysbox deployment
    sysboxBase: public.ecr.aws/v1i4e1r2/ubuntu-devbox-base:base-2024-12-17--06-31--c705618e0d55
    ## @param workspace.baseImages.kataBase Base image for kata deployment
    kataBase: public.ecr.aws/v1i4e1r2/kata-devbox-base:base-2025-03-19--18-01--29221879bc90
    ## @param workspace.baseImages.cliImage Base image for cli deployment
    cliImage: public.ecr.aws/v1i4e1r2/dzcmd:9a8e088


## @section Global Configuration
## @param global.podAnnotations Global pod annotations
global:
  podAnnotations: {}

## @section Service Account Configuration
serviceAccount:
  ## @param serviceAccount.create Specifies whether a service account should be created
  create: false
  ## @param serviceAccount.annotations Annotations to add to the service account
  annotations: {}
  ## @param serviceAccount.name The name of the service account to use
  name: "devzero"

## @section API Gateway Configuration
gateway:
  ## @param gateway.replicas Number of replicas for Api Gateway
  replicas: 3
  ## @param gateway.imageName Image name for the API Gateway
  imageName: "api-gateway"
  ## @param gateway.schedulerName Optionally set the scheduler for pods
  schedulerName: ""
  ## @param gateway.priorityClassName Optionally set the name of the PriorityClass for pods
  priorityClassName: ""
  ## @param gateway.nodeSelector NodeSelector to pin pods to certain set of nodes
  nodeSelector: {}
  ## @param gateway.affinity Pod affinity settings
  affinity: {}
  ## @param gateway.tolerations Pod tolerations
  tolerations: []
  ## @param gateway.podLabels Pod labels
  podLabels: {}
  ## @param gateway.podAnnotations Pod annotations
  podAnnotations: {}
  ## @param gateway.annotations Annotations
  annotations: {}

  ## @subsection Gateway Autoscaling Configuration
  autoscaling:
    ## @param gateway.autoscaling.enabled Enable autoscaling for Api Gateway
    enabled: false
    ## @param gateway.autoscaling.minReplicas Minimum autoscaling replicas for Api Gateway
    minReplicas: 1
    ## @param gateway.autoscaling.maxReplicas Maximum autoscaling replicas for Api Gateway
    maxReplicas: 3
    ## @param gateway.autoscaling.targetCPUUtilizationPercentage Target CPU utilisation percentage for Api Gateway
    targetCPUUtilizationPercentage: 60
    ## @param gateway.autoscaling.targetMemoryUtilizationPercentage Target memory utilisation percentage for Api Gateway
    targetMemoryUtilizationPercentage: 80

  ## @subsection Gateway Resources Configuration
  resources:
    # @param gateway.resources.limits Resource limits for Api Gateway
    limits:
      ## @param gateway.resources.limits.cpu CPU limit for Api Gateway
      cpu: 1000m
      ## @param gateway.resources.limits.memory Memory limit for Api Gateway
      memory: 1Gi
    # @param gateway.resources.requests Resource requests for Api Gateway
    requests:
      ## @param gateway.resources.requests.cpu CPU request for Api Gateway
      cpu: 100m
      ## @param gateway.resources.requests.memory Memory request for Api Gateway
      memory: 128Mi

  ## @subsection Gateway Service Configuration
  service:
    ## @param gateway.service.port Port of the Api Gateway service
    port: 8443
    ## @param gateway.service.metricsPort Port of the Api Gateway Metrics service
    metricsPort: 9090
    ## @param gateway.service.type Type of the Api Gateway service
    type: ClusterIP
    ## @param gateway.service.annotations Annotations for the Api Gateway service
    annotations: {}
    ## @param gateway.service.labels Labels for the Api Gateway service
    labels: {}

  ## @subsection Gateway Ingress Configuration
  ingress:
    ## @param gateway.ingress.enabled Specify if the Api Gateway Ingress is enabled
    enabled: enable
    ## @param gateway.ingress.ingressClassName Ingress Class Name. May be required for k8s >= 1.18
    ingressClassName: "nginx"
    # @param gateway.ingress.annotations Annotations for ingress
    annotations:
      ## @param gateway.ingress.annotations.cert-manager.io/cluster-issuer Certificate issuer for ingress
      cert-manager.io/cluster-issuer: "letsencrypt-self-hosted"
      ## @param gateway.ingress.annotations.nginx.ingress.kubernetes.io/force-ssl-redirect Force SSL redirect for ingress
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # @param gateway.ingress.tls TLS configuration
    tls:
      ## @param gateway.ingress.tls[0].secretName TLS secret name for ingress
    - secretName: devzero-api-tls
        ## @param gateway.ingress.tls[0].hosts List of TLS hosts for ingress
      hosts:
      - "api.{{ .Values.domain }}"
    # @param gateway.ingress.hosts Hosts configuration
    hosts:
      ## @param gateway.ingress.hosts[0].host Host name for ingress
    - host: "api.{{ .Values.domain }}"
        # @param gateway.ingress.hosts[0].paths List of path configurations for ingress
      paths:
          ## @param gateway.ingress.hosts[0].paths[0].path Path for ingress route
      - path: /
            ## @param gateway.ingress.hosts[0].paths[0].pathType Path type for ingress route
        pathType: Prefix

## @section Backend Configuration
backend:
  ## @subsection Backend Service Account Configuration
  serviceAccount:
    ## @param backend.serviceAccount.create Specifies whether a service account should be created
    create: true

  ## @param backend.replicas Number of replicas for Backend
  replicas: 1

  ## @param backend.imageName Image name for Backend service
  imageName: "backend"

  ## @param backend.schedulerName Optionally set the scheduler for pods
  schedulerName: ""

  ## @param backend.priorityClassName Optionally set the name of the PriorityClass for pods
  priorityClassName: ""

  ## @param backend.nodeSelector NodeSelector to pin pods to certain set of nodes
  nodeSelector: {}

  ## @param backend.affinity Pod affinity settings
  affinity: {}

  ## @param backend.tolerations Pod tolerations
  tolerations: []

  ## @param backend.podLabels Pod labels
  podLabels: {}

  ## @param backend.podAnnotations Pod annotations
  podAnnotations: {}

  ## @param backend.annotations Annotations
  annotations: {}

  ## @param backend.enableOpenSignup Whether or not to allow people sign up without an invite
  enableOpenSignup: false

  ## @subsection Backend Redis Configuration
  redis:
    ## @param backend.redis.url Redis URL for Backend
    url: redis://redis-headless:6379/1
    ## @param backend.redis.password Redis password for Backend
    password: ""

  ## @subsection Backend MongoDB Configuration
  mongo:
    ## @param backend.mongo.url MongoDB connection URL
    url: "mongodb://devzero:backend@mongodb-0.mongodb-headless:27017/backend?directConnection=true"

  ## @subsection Backend Hydra Configuration
  hydra:
    ## @param backend.hydra.apiKey API key for Hydra service
    apiKey: ""

  ## @subsection Backend LogSrv Configuration
  logsrv:
    ## @param backend.logsrv.queue LogSrv queue URL
    queue: http://elasticmq:9324/queue/logsrv.fifo
    ## @param backend.logsrv.region LogSrv region
    region: "elasticmq"

  ## @subsection Backend GitHub Configuration
  github:
    ## @param backend.github.appId GitHub App ID
    appId: 0
    ## @param backend.github.appPrivateKey GitHub App Private Key
    appPrivateKey: ""
    ## @param backend.github.appUrl GitHub App url
    appUrl: ""
    ## @param backend.github.clientId GitHub App client ID
    clientId: ""
    ## @param backend.github.clientSecret GitHub App client secret
    clientSecret: ""
    ## @param backend.github.permitUrl GitHub App permit url
    permitUrl: ""
    ## @param backend.github.tokenExchangeUrl GitHub App token url
    tokenExchangeUrl: ""
    ## @param backend.github.enterpriseUrl GitHub enterprise server url
    enterpriseUrl: ""

  ## @subsection Backend Mimir Configuration
  mimir:
    ## @param backend.mimir.url Mimir service URL
    url: http://mimir:8080/prometheus

  ## @subsection Backend Hibernation Configuration
  hibernation:
    ## @param backend.hibernation.enabled Enable hibernation feature
    enabled: false

  ## @subsection Backend SendGrid Configuration
  sendgrid:
    ## @param backend.sendgrid.apiKey SendGrid API Key
    apiKey: test-key

  ## @subsection Backend Grafana Configuration
  grafana:
    ## @param backend.grafana.enabled Enable Grafana integration
    enabled: true
    ## @param backend.grafana.datasourceId Main datasource ID
    datasourceId: "mimir"
    ## @param backend.grafana.arcDatasourceId ARC datasource ID
    arcDatasourceId: "prometheus"
    ## @param backend.grafana.doraDatasourceId DORA metrics datasource ID
    doraDatasourceId: "pulse"
    ## @param backend.grafana.odaDatasourceId ODA metrics datasource ID
    odaDatasourceId: "timescale"
    ## @param backend.grafana.password Grafana password
    password: prom-operator

  ## @subsection Backend Storage Configuration
  storage:
    ## @param backend.storage.allowedTeam Allowed team ID for storage access
    allowedTeam: team-example-id
    ## @subsection Backend Ceph Storage Configuration
    ceph:
      ## @param backend.storage.ceph.clusterId Ceph cluster ID
      clusterId: ""
      ## @param backend.storage.ceph.filesystemName Ceph filesystem name
      filesystemName: cephfs
      ## @param backend.storage.ceph.filesystemPath Ceph filesystem path
      filesystemPath: cephfs
      ## @param backend.storage.ceph.monitorAddress Ceph monitor address
      monitorAddress: /volumes/cache
      ## @param backend.storage.ceph.userCredentials Ceph user credentials
      userCredentials: ""
      ## @param backend.storage.ceph.username Ceph username
      username: vuser

  ## @param backend.mainTeamId Main team ID
  mainTeamId: ""

  ## @param backend.licenseKey License key
  licenseKey: ""
  
  ## @param backend.arch Architecture type
  arch: amd64

  ## @param backend.cortex.key Cortex key
  cortex:
    key: ""

  ## @subsection Backend Autoscaling Configuration
  autoscaling:
    ## @param backend.autoscaling.enabled Enable autoscaling for Backend
    enabled: false
    ## @param backend.autoscaling.minReplicas Minimum autoscaling replicas for Backend
    minReplicas: 1
    ## @param backend.autoscaling.maxReplicas Maximum autoscaling replicas for Backend
    maxReplicas: 3
    ## @param backend.autoscaling.targetCPUUtilizationPercentage Target CPU utilisation percentage for Backend
    targetCPUUtilizationPercentage: 60
    ## @param backend.autoscaling.targetMemoryUtilizationPercentage Target memory utilisation percentage for Backend
    targetMemoryUtilizationPercentage: 80

  ## @subsection Backend Resources Configuration
  resources:
    # @param backend.resources.limits Resource limits for Backend
    limits:
      ## @param backend.resources.limits.cpu CPU resource limits for Backend
      cpu: 1000m
      ## @param backend.resources.limits.memory Memory resource limits for Backend
      memory: 1Gi
    # @param backend.resources.requests Resource requests for Backend
    requests:
      ## @param backend.resources.requests.cpu CPU resource requests for Backend
      cpu: 100m
      ## @param backend.resources.requests.memory Memory resource requests for Backend
      memory: 128Mi

  ## @subsection Backend Service Configuration
  service:
    ## @param backend.service.port Port of the Backend service
    port: 8443
    ## @param backend.service.metricsPort Port of the Backend Metrics service
    metricsPort: 9090
    ## @param backend.service.type Type of the Backend service
    type: ClusterIP
    ## @param backend.service.annotations Annotations for the Backend service
    annotations: {}
    ## @param backend.service.labels Labels for the Backend service
    labels: {}

## @section Hydra Configuration
hydra:
  ## @param hydra.replicas Number of replicas for Hydra
  replicas: 1

  ## @param hydra.imageName Image name for Hydra service
  imageName: "hydra"

  ## @param hydra.schedulerName Optionally set the scheduler for pods
  schedulerName: ""

  ## @param hydra.priorityClassName Optionally set the name of the PriorityClass for pods
  priorityClassName: ""

  ## @param hydra.nodeSelector NodeSelector to pin pods to certain set of nodes
  nodeSelector: {}

  ## @param hydra.affinity Pod affinity settings
  affinity: {}

  ## @param hydra.tolerations Pod tolerations
  tolerations: []

  ## @param hydra.podLabels Pod labels
  podLabels: {}

  ## @param hydra.podAnnotations Pod annotations
  podAnnotations: {}

  ## @param hydra.annotations Annotations
  annotations: {}

  ## @param hydra.job Enable job mode
  job: true

  ## @subsection Hydra Postgres Configuration
  postgres:
    ## @param hydra.postgres.host PostgreSQL host
    host: "devzero-db-postgresql-hl"
    ## @param hydra.postgres.port PostgreSQL port
    port: 5432
    ## @param hydra.postgres.name PostgreSQL database name
    name: "hydra"
    ## @param hydra.postgres.user PostgreSQL user
    user: "hydra"
    ## @param hydra.postgres.password PostgreSQL password
    password: "hydra"
  ## @subsection Hydra Redis Configuration
  redis:
    ## @param hydra.redis.url Redis URL for Hydra
    url: redis://redis-headless:6379/2
  ## @subsection Hydra CIDR Configuration
  cidr:
    ## @param hydra.cidr.v6 IPv6 CIDR range
    v6: fd20:1984:42::/48
    ## @param hydra.cidr.v4 IPv4 CIDR range
    v4: 100.64.0.0/10
  ## @subsection Hydra DERP Configuration
  derp:
    server:
      ## @param hydra.derp.server.enabled Enable DERP server
      enabled: false
      ## @param hydra.derp.server.embeddedDerpRegion Enable embedded DERP region
      embeddedDerpRegion: false

      ## @param hydra.derp.server.regionId Embedded derp region id
      regionId: 999
      ## @param hydra.derp.server.regionCode Embedded derp region code
      regionCode: "headscale"
      ## @param hydra.derp.server.regionName Embedded derp region name
      regionName: "Headscale Embedded DERP"
      ## @param hydra.derp.server.stunListenAddr Enable embedded DERP stun listen address
      stunListenAddr: "0.0.0.0:3478"
      ## @param hydra.derp.server.privateKey Enable embedded DERP private key
      privateKey: ""
      ## @param hydra.derp.server.insecure Enable embedded DERP with insecure connection
      insecure: true

    ## @param hydra.derp.urls DERP server URLs
    urls:
    - "https://controlplane.tailscale.com/derpmap/default"
    # for a custom derp server, include the following after deploying the server
    # customderp: |
    #   {
    #       "Regions": {
    #         "901": {
    #           "RegionID": 901,
    #           "RegionCode": "nyc", // can be set to anything, keep human readable
    #           "RegionName": "New York City", // change to derp server deployment geolocation
    #           "Latitude": 40.7128, // change to derp server deployment geolocation
    #           "Longitude": -74.006, // change to derp server deployment geolocation
    #           "Nodes": [
    #             {
    #               "Name": "901a", 
    #               "RegionID": 901,
    #               "HostName": "your.hostname.com", // for private IP deployments, use the private IP of the VPS
    #               "IPv4": "1.1.1.1", // IP address of the VPS
    #               "IPv6": "1608:f740:f::ab", // IPv6 address of the VPS
    #               "InsecureForTests": true // set this true for private IP deploys, false otherwise
    #             }
    #           ]
    #         }
    #     }
    #   }
    ## @param hydra.derp.autoUpdateEnabled endable derp map updates
    autoUpdateEnabled: true
    ## @param hydra.derp.updateFrequency Update frequency for DERP
    updateFrequency: "24h"

  ## @subsection Hydra Autoscaling Configuration
  autoscaling:
    ## @param hydra.autoscaling.enabled Enable autoscaling for Hydra
    enabled: false
    ## @param hydra.autoscaling.minReplicas Minimum autoscaling replicas for Hydra
    minReplicas: 1
    ## @param hydra.autoscaling.maxReplicas Maximum autoscaling replicas for Hydra
    maxReplicas: 3
    ## @param hydra.autoscaling.targetCPUUtilizationPercentage Target CPU utilisation percentage for Hydra
    targetCPUUtilizationPercentage: 60
    ## @param hydra.autoscaling.targetMemoryUtilizationPercentage Target memory utilisation percentage for Hydra
    targetMemoryUtilizationPercentage: 80

  ## @subsection Hydra Resources Configuration
  resources:
    # @param hydra.resources.limits Resource limits for Hydra
    limits:
      ## @param hydra.resources.limits.cpu CPU resource limits for Hydra
      cpu: 1000m
      ## @param hydra.resources.limits.memory Memory resource limits for Hydra
      memory: 1Gi
    # @param hydra.resources.requests Resource requests for Hydra
    requests:
      ## @param hydra.resources.requests.cpu CPU resource requests for Hydra
      cpu: 100m
      ## @param hydra.resources.requests.memory Memory resource requests for Hydra
      memory: 128Mi

  ## @subsection Hydra Service Configuration
  service:
    ## @param hydra.service.port Port of the Hydra service
    port: 8443
    ## @param hydra.service.metricsPort Port of the Hydra Metrics service
    metricsPort: 9090
    ## @param hydra.service.type Type of the Hydra service
    type: ClusterIP
    ## @param hydra.service.annotations Annotations for the Hydra service
    annotations: {}
    ## @param hydra.service.labels Labels for the Hydra service
    labels: {}

  ## @subsection Hydra Ingress Configuration
  ingress:
    ## @param hydra.ingress.enabled Specify if the Hydra Ingress is enabled
    enabled: true
    ## @param hydra.ingress.ingressClassName Ingress Class Name
    ingressClassName: "nginx"
    # @param hydra.ingress.annotations Annotations for Hydra ingress
    annotations:
      ## @param hydra.ingress.annotations.cert-manager.io/cluster-issuer Certificate issuer for ingress
      cert-manager.io/cluster-issuer: "letsencrypt-self-hosted"
      ## @param hydra.ingress.annotations.nginx.ingress.kubernetes.io/force-ssl-redirect Force SSL redirect for ingress
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # @param hydra.ingress.tls TLS configuration for Hydra ingress
    tls:
      ## @param hydra.ingress.tls[0].secretName TLS secret name for ingress
    - secretName: devzero-hydra-tls
        ## @param hydra.ingress.tls[0].hosts List of TLS hosts for ingress
      hosts:
      - "hydra.{{ .Values.domain }}"
    # @param hydra.ingress.hosts Hosts configuration for Hydra ingress
    hosts:
      ## @param hydra.ingress.hosts[0].host Host name for ingress
    - host: "hydra.{{ .Values.domain }}"
        # @param hydra.ingress.hosts[0].paths List of path configurations for ingress
      paths:
          ## @param hydra.ingress.hosts[0].paths[0].path Path for ingress route
      - path: /
            ## @param hydra.ingress.hosts[0].paths[0].pathType Path type for ingress route
        pathType: Prefix

## @section LogSrv Configuration
logsrv:
  ## @param logsrv.replicas Number of replicas for LogSrv
  replicas: 1

  ## @param logsrv.imageName Image name for LogSrv
  imageName: "logsrv"

  ## @param logsrv.schedulerName Scheduler name for LogSrv pods
  schedulerName: ""

  ## @param logsrv.priorityClassName Priority class name for LogSrv pods
  priorityClassName: ""

  ## @param logsrv.nodeSelector Node selector for LogSrv pods
  nodeSelector: {}

  ## @param logsrv.affinity Affinity settings for LogSrv pods
  affinity: {}

  ## @param logsrv.tolerations Tolerations for LogSrv pods
  tolerations: []

  ## @param logsrv.podLabels Pod labels for LogSrv
  podLabels: {}

  ## @param logsrv.podAnnotations Pod annotations for LogSrv
  podAnnotations: {}

  ## @param logsrv.annotations Annotations for LogSrv
  annotations: {}

  ## @subsection LogSrv Queue Configuration
  queue:
    ## @param logsrv.queue.url Queue URL for LogSrv
    url: http://elasticmq:9324/queue/logsrv.fifo

  ## @subsection LogSrv Postgres Configuration
  postgres:
    ## @param logsrv.postgres.url PostgreSQL URL for LogSrv
    url: postgresql://logsrv:logsrv@devzero-db-postgresql-hl:5432/logsrv
    ## @param logsrv.postgres.password PostgreSQL password for LogSrv
    password: logsrv

  ## @param logsrv.refreshJwksTimer JWKS refresh timer in seconds
  refreshJwksTimer: 3600

  ## @subsection LogSrv Autoscaling Configuration
  autoscaling:
    ## @param logsrv.autoscaling.enabled Enable autoscaling for LogSrv
    enabled: false
    ## @param logsrv.autoscaling.minReplicas Minimum replicas for LogSrv
    minReplicas: 1
    ## @param logsrv.autoscaling.maxReplicas Maximum replicas for LogSrv
    maxReplicas: 3
    ## @param logsrv.autoscaling.targetCPUUtilizationPercentage Target CPU utilization percentage
    targetCPUUtilizationPercentage: 60
    ## @param logsrv.autoscaling.targetMemoryUtilizationPercentage Target memory utilization percentage
    targetMemoryUtilizationPercentage: 80

  ## @subsection LogSrv Resources Configuration
  resources:
    # @param logsrv.resources.limits Resource limits for LogSrv
    limits:
      ## @param logsrv.resources.limits.cpu CPU resource limits for LogSrv  
      cpu: 1000m
      ## @param logsrv.resources.limits.memory Memory resource limits for LogSrv
      memory: 1Gi
    # @param logsrv.resources.requests Resource requests for LogSrv
    requests:
      ## @param logsrv.resources.requests.cpu CPU resource requests for LogSrv
      cpu: 100m
      ## @param logsrv.resources.requests.memory Memory resource requests for LogSrv
      memory: 128Mi

  ## @subsection LogSrv Service Configuration
  service:
    ## @param logsrv.service.port Service port for LogSrv
    port: 8443
    ## @param logsrv.service.metricsPort Metrics port for LogSrv
    metricsPort: 9090
    ## @param logsrv.service.type Service type for LogSrv
    type: ClusterIP
    ## @param logsrv.service.annotations Service annotations for LogSrv
    annotations: {}
    ## @param logsrv.service.labels Service labels for LogSrv
    labels: {}

  ## @subsection LogSrv Ingress Configuration
  ingress:
    ## @param logsrv.ingress.enabled Enable ingress for LogSrv
    enabled: true
    ## @param logsrv.ingress.ingressClassName Ingress class name for LogSrv
    ingressClassName: "nginx"
    # @param logsrv.ingress.annotations Ingress annotations for LogSrv
    annotations:
      ## @param logsrv.ingress.annotations.cert-manager.io/cluster-issuer Certificate issuer for ingress
      cert-manager.io/cluster-issuer: "letsencrypt-self-hosted"
      ## @param logsrv.ingress.annotations.nginx.ingress.kubernetes.io/force-ssl-redirect Force SSL redirect for ingress
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # @param logsrv.ingress.tls TLS configuration for LogSrv ingress
    tls:
      ## @param logsrv.ingress.tls[0].secretName TLS secret name for ingress
    - secretName: devzero-logsrv-tls
        ## @param logsrv.ingress.tls[0].hosts List of TLS hosts for ingress
      hosts:
      - "logsrv.{{ .Values.domain }}"
    # @param logsrv.ingress.hosts Hosts configuration for LogSrv ingress
    hosts:
      ## @param logsrv.ingress.hosts[0].host Host name for ingress
    - host: "logsrv.{{ .Values.domain }}"
        # @param logsrv.ingress.hosts[0].paths List of path configurations for ingress
      paths:
          ## @param logsrv.ingress.hosts[0].paths[0].path Path for ingress route
      - path: /
            ## @param logsrv.ingress.hosts[0].paths[0].pathType Path type for ingress route
        pathType: Prefix

## @section Polland Configuration
polland:
  ## @subsection Polland Service Account Configuration
  serviceAccount:
    ## @param polland.serviceAccount.create Specifies whether a service account should be created
    create: true

  ## @subsection Polland Worker Configuration
  worker:
    ## @param polland.worker.replicas Number of replicas for Polland Worker
    replicas: 3

    ## @param polland.worker.terminationGracePeriodSeconds Termination grace period in seconds
    terminationGracePeriodSeconds: 3600

    # @param polland.worker.queues Queue configurations for workers
    queues:
      ## @param polland.worker.queues[0].name Configuration for the fast queue
    - name: fast
        ## @param polland.worker.queues[0].replicaCount Number of replicas for queue
      replicaCount: 1
        # @param polland.worker.queues[0].autoscaling Autoscaling configuration for queue
      autoscaling:
          ## @param polland.worker.queues[0].autoscaling.enabled Enable autoscaling for queue
        enabled: false
          ## @param polland.worker.queues[0].autoscaling.minReplicas Minimum autoscaling replicas for queue
        minReplicas: 3
          ## @param polland.worker.queues[0].autoscaling.maxReplicas Maximum autoscaling replicas for queue
        maxReplicas: 5
      ## @param polland.worker.queues[1].name Configuration for the build queue
    - name: build
        ## @param polland.worker.queues[1].replicaCount Number of replicas for queue
      replicaCount: 1
        # @param polland.worker.queues[1].autoscaling Autoscaling configuration for queue
      autoscaling:
          ## @param polland.worker.queues[1].autoscaling.enabled Enable autoscaling for queue
        enabled: false
          ## @param polland.worker.queues[1].autoscaling.minReplicas Minimum autoscaling replicas for queue
        minReplicas: 3
          ## @param polland.worker.queues[1].autoscaling.maxReplicas Maximum autoscaling replicas for queue
        maxReplicas: 5
      ## @param polland.worker.queues[2].name Configuration for the workload queue
    - name: workload
        ## @param polland.worker.queues[2].replicaCount Number of replicas for queue
      replicaCount: 1
        # @param polland.worker.queues[2].autoscaling Autoscaling configuration for queue
      autoscaling:
          ## @param polland.worker.queues[2].autoscaling.enabled Enable autoscaling for queue
        enabled: false
          ## @param polland.worker.queues[2].autoscaling.minReplicas Minimum autoscaling replicas for queue
        minReplicas: 3
          ## @param polland.worker.queues[2].autoscaling.maxReplicas Maximum autoscaling replicas for queue
        maxReplicas: 5
      ## @param polland.worker.queues[3].name Configuration for the cluster queue
    - name: cluster
        ## @param polland.worker.queues[3].replicaCount Number of replicas for queue
      replicaCount: 1
        # @param polland.worker.queues[3].autoscaling Autoscaling configuration for queue
      autoscaling:
          ## @param polland.worker.queues[3].autoscaling.enabled Enable autoscaling for queue
        enabled: false
          ## @param polland.worker.queues[3].autoscaling.minReplicas Minimum autoscaling replicas for queue
        minReplicas: 3
          ## @param polland.worker.queues[3].autoscaling.maxReplicas Maximum autoscaling replicas for queue
        maxReplicas: 5
      ## @param polland.worker.queues[4].name Configuration for the hibernation queue
    - name: hibernation
        ## @param polland.worker.queues[4].replicaCount Number of replicas for queue
      replicaCount: 1
        # @param polland.worker.queues[4].autoscaling Autoscaling configuration for queue
      autoscaling:
          ## @param polland.worker.queues[4].autoscaling.enabled Enable autoscaling for queue
        enabled: false
          ## @param polland.worker.queues[4].autoscaling.minReplicas Minimum autoscaling replicas for queue
        minReplicas: 1
          ## @param polland.worker.queues[4].autoscaling.maxReplicas Maximum autoscaling replicas for queue
        maxReplicas: 1

  ## @subsection Polland Beat Configuration
  beat:
    ## @param polland.beat.replicas Number of replicas for Polland Beat
    replicas: 1

  ## @subsection Polland Flower Configuration
  flower:
    ## @param polland.flower.replicas Number of replicas for Polland Flower
    replicas: 1
    ## @subsection Polland Flower Service Configuration
    service:
      ## @param polland.flower.service.port Port for Flower service
      port: 5555
      ## @param polland.flower.service.type Type of Flower service
      type: ClusterIP

  ## @subsection Polland Celery Exporter Configuration
  celeryExporter:
    ## @param polland.celeryExporter.replicas Number of replicas for Celery Exporter
    replicas: 1

  ## @param polland.replicas Number of replicas for Polland
  replicas: 1

  ## @param polland.imageName Image name for Polland
  imageName: "polland"

  ## @param polland.schedulerName Scheduler name for Polland pods
  schedulerName: ""

  ## @param polland.priorityClassName Priority class name for Polland pods
  priorityClassName: ""

  ## @param polland.nodeSelector Node selector for Polland pods
  nodeSelector: {}

  ## @param polland.affinity Affinity settings for Polland pods
  affinity: {}

  ## @param polland.tolerations Tolerations for Polland pods
  tolerations: []

  ## @param polland.podLabels Pod labels for Polland
  podLabels: {}

  ## @param polland.podAnnotations Pod annotations for Polland
  podAnnotations: {}

  ## @param polland.annotations Annotations for Polland
  annotations: {}

  ## @param polland.vaultEndpoint vaultEndpoint for Polland
  vaultEndpoint: "http://vault:8200"

  ## @subsection Polland Environment Configuration
  env:
    ## @param polland.env.USE_POSTGRES_DB Use PostgreSQL database
    USE_POSTGRES_DB: "true"
    ## @param polland.env.POSTGRES_PORT PostgreSQL port
    POSTGRES_PORT: "5432"
    ## @param polland.env.POSTGRES_DB PostgreSQL database name
    POSTGRES_DB: "polland"
    ## @param polland.env.POSTGRES_USER PostgreSQL user
    POSTGRES_USER: "polland"
    ## @param polland.env.POSTGRES_PASSWORD PostgreSQL password
    POSTGRES_PASSWORD: "polland"
    ## @param polland.env.POSTGRES_HOST PostgreSQL host
    POSTGRES_HOST: "devzero-db-postgresql-hl"

    ## @param polland.env.REDIS_URL Redis URL
    REDIS_URL: "redis://redis-headless:6379/0"

    ## @param polland.env.USE_DOCKER Use Docker
    USE_DOCKER: "yes"
    ## @param polland.env.CONN_MAX_AGE Connection max age
    CONN_MAX_AGE: "60"
    ## @param polland.env.DJANGO_ALLOWED_HOSTS Allowed hosts for Django
    DJANGO_ALLOWED_HOSTS: "*"
    ## @param polland.env.DJANGO_SETTINGS_MODULE Django settings module
    DJANGO_SETTINGS_MODULE: "config.settings.production"
    ## @param polland.env.DJANGO_SECRET_KEY Django secret key
    DJANGO_SECRET_KEY: "super_secret_key"
    ## @param polland.env.CELERY_FLOWER_USER Celery Flower user
    CELERY_FLOWER_USER: "devzero"
    ## @param polland.env.CELERY_FLOWER_PASSWORD Celery Flower password
    CELERY_FLOWER_PASSWORD: "devzero"

    ## @param polland.env.SELF_HOSTED Self-hosted mode
    SELF_HOSTED: "True"

    ## @param polland.env.KATA_NODE_SELECTOR Node selector for pods that use Kata runtime
    KATA_NODE_SELECTOR: "node-role.kubernetes.io/kata-devpod-node"

    ## @param polland.env.DEVPOD_NODE_SELECTOR Node selector for containers that support workspaces
    DEVPOD_NODE_SELECTOR: "node-role.kubernetes.io/devpod-node"

    ## @param polland.env.USE_INSECURE_REGISTRY Use insecure registry
    USE_INSECURE_REGISTRY: "True"
    ## @param polland.env.USE_ECR_REGISTRY Use ECR registry
    USE_ECR_REGISTRY: "False"
    ## @param polland.env.USE_LOCAL_LOGSRV Use local LogSrv
    USE_LOCAL_LOGSRV: "True"

    ## @param polland.env.VAULT_AUTH_METHOD Vault auth method
    VAULT_AUTH_METHOD: "kubernetes"
    ## @param polland.env.VAULT_SECRETS_MOUNT_POINT Vault secrets mount point
    VAULT_SECRETS_MOUNT_POINT: "vault-csi-writer"
    ## @param polland.env.LOGSRV_DEFAULT_QUEUE Default LogSrv queue URL
    LOGSRV_DEFAULT_QUEUE: "http://elasticmq:9324/queue/logsrv.fifo"
    ## @param polland.env.LOGSRV_DEFAULT_REGION Default LogSrv region
    LOGSRV_DEFAULT_REGION: "elasticmq"

    ## @param polland.env.DOCKER_REGISTRY Use custom docker registry
    DOCKER_REGISTRY: "docker-registry.{{ .Values.domain }}"

    ## @param polland.env.DEVBOX_INGRESS_CLASS_NAME Use custom nginx ingress controller
    DEVBOX_INGRESS_CLASS_NAME: "devzero-data-ingress"

    ## @param polland.env.ALLOW_POD_EVICTION_ANNOTATION JSON style annotations to allow pod eviction
    # ClusterAutoscaler https://github.com/kubernetes/autoscaler/blob/master/cluster-autoscaler/FAQ.md#what-types-of-pods-can-prevent-ca-from-removing-a-node
    ALLOW_POD_EVICTION_ANNOTATION: '{"cluster-autoscaler.kubernetes.io/safe-to-evict":
      "true"}'
    # karpenter < 1.0 https://karpenter.sh/v1.0/upgrading/v1-migration/
    # ALLOW_POD_EVICTION_ANNOTATION: '{"karpenter.sh/do-not-evict": "false"}'
    # karpenter > 1.0 https://karpenter.sh/v1.0/upgrading/v1-migration/
    # ALLOW_POD_EVICTION_ANNOTATION: '{"karpenter.sh/do-not-disrupt": "true"}'


    ## @param polland.env.DISALLOW_POD_EVICTION_ANNOTATION JSON style annotations to disallow pod eviction
    # ClusterAutoscaler https://github.com/kubernetes/autoscaler/blob/master/cluster-autoscaler/FAQ.md#what-types-of-pods-can-prevent-ca-from-removing-a-node
    DISALLOW_POD_EVICTION_ANNOTATION: '{"cluster-autoscaler.kubernetes.io/safe-to-evict":
      "false"}'
    # karpenter < 1.0 https://karpenter.sh/v1.0/upgrading/v1-migration/
    # DISALLOW_POD_EVICTION_ANNOTATION: '{"karpenter.sh/do-not-evict": "true"}'
    # karpenter > 1.0 https://karpenter.sh/v1.0/upgrading/v1-migration/
    # DISALLOW_POD_EVICTION_ANNOTATION: '{"karpenter.sh/do-not-disrupt": "false"}'

    ## @param polland.env.VIRTUAL_CLUSTER_AFFINITY JSON style affinity for all virtual cluster pods
    VIRTUAL_CLUSTER_AFFINITY: "{}"

    ## @param polland.env.DEVBOX_AFFINITY JSON style affinity for all devbox pods
    DEVBOX_AFFINITY: "{}"

    ## @param polland.env.DEVBOX_TOLERATIONS JSON style tolerations for all devbox pods
    DEVBOX_TOLERATIONS: "[]"

    ## @param polland.env.DEVBOX_NODE_SELECTORS JSON style nodeSelector for all devbox pods
    DEVBOX_NODE_SELECTORS: "{}"

  ## @subsection Polland Autoscaling Configuration
  autoscaling:
    ## @param polland.autoscaling.enabled Enable autoscaling for Polland
    enabled: false
    ## @param polland.autoscaling.minReplicas Minimum autoscaling replicas for Polland
    minReplicas: 1
    ## @param polland.autoscaling.maxReplicas Maximum autoscaling replicas for Polland
    maxReplicas: 3
    ## @param polland.autoscaling.targetCPUUtilizationPercentage Target CPU utilisation percentage for Polland
    targetCPUUtilizationPercentage: 60
    ## @param polland.autoscaling.targetMemoryUtilizationPercentage Target memory utilisation percentage for Polland
    targetMemoryUtilizationPercentage: 80

  ## @subsection Polland Resources Configuration
  resources:
    # @param polland.resources.limits Resource limits for Polland
    limits:
      ## @param polland.resources.limits.cpu CPU resource limits for Polland
      cpu: 1000m
      ## @param polland.resources.limits.memory Memory resource limits for Polland
      memory: 1Gi
    # @param polland.resources.requests Resource requests for Polland
    requests:
      ## @param polland.resources.requests.cpu CPU resource requests for Polland
      cpu: 100m
      ## @param polland.resources.requests.memory Memory resource requests for Polland
      memory: 128Mi

  ## @subsection Polland Service Configuration
  service:
    ## @param polland.service.port Port of the Polland service
    port: 8000
    ## @param polland.service.metricsPort Port of the Polland Metrics service
    metricsPort: 9090
    ## @param polland.service.type Type of the Polland service
    type: ClusterIP
    ## @param polland.service.annotations Annotations for the Polland service
    annotations: {}
    ## @param polland.service.labels Labels for the Polland service
    labels: {}

## @section Pulse Configuration
pulse:
  ## @param pulse.replicas Number of replicas for Pulse
  replicas: 1

  ## @param pulse.imageName Image name for Pulse
  imageName: "pulse"

  ## @param pulse.schedulerName Scheduler name for Pulse pods
  schedulerName: ""

  ## @param pulse.priorityClassName Priority class name for Pulse pods
  priorityClassName: ""

  ## @param pulse.nodeSelector Node selector for Pulse pods
  nodeSelector: {}

  ## @param pulse.affinity Affinity settings for Pulse pods
  affinity: {}

  ## @param pulse.tolerations Tolerations for Pulse pods
  tolerations: []

  ## @param pulse.podLabels Pod labels for Pulse
  podLabels: {}

  ## @param pulse.podAnnotations Pod annotations for Pulse
  podAnnotations: {}

  ## @param pulse.annotations Annotations for Pulse
  annotations: {}

  ## @subsection Pulse Postgres Configuration
  postgres:
    ## @param pulse.postgres.password PostgreSQL password for Pulse
    password: pulse
    ## @param pulse.postgres.user PostgreSQL user for Pulse
    user: pulse
    ## @param pulse.postgres.host PostgreSQL host for Pulse
    host: timescaledb-cluster-rw
    ## @param pulse.postgres.port PostgreSQL port for Pulse
    port: 5432
    ## @param pulse.postgres.database PostgreSQL database for Pulse
    database: pulse
    ## @param pulse.postgres.ssl SSL mode for PostgreSQL
    ssl: allow

  ## @subsection Pulse MySQL Configuration
  mysql:
    ## @param pulse.mysql.user MySQL user for Pulse
    user: pulse
    ## @param pulse.mysql.password MySQL password for Pulse
    password: pulse
    ## @param pulse.mysql.host MySQL host for Pulse
    host: mysql-pulse-headless
    ## @param pulse.mysql.port MySQL port for Pulse
    port: 3306
    ## @param pulse.mysql.database MySQL database for Pulse
    database: pulse

  ## @subsection Pulse Devlake Configuration
  devlake:
    ## @param pulse.devlake.secret Devlake secret for Pulse
    secret: ""

  ## @subsection Pulse OpenAPI Configuration
  openApi:
    ## @param pulse.openApi.token OpenAPI token for Pulse
    token: ""

  ## @subsection Pulse Autoscaling Configuration
  autoscaling:
    ## @param pulse.autoscaling.enabled Enable autoscaling for Pulse
    enabled: false
    ## @param pulse.autoscaling.minReplicas Minimum autoscaling replicas for Pulse
    minReplicas: 1
    ## @param pulse.autoscaling.maxReplicas Maximum autoscaling replicas for Pulse
    maxReplicas: 3
    ## @param pulse.autoscaling.targetCPUUtilizationPercentage Target CPU utilisation percentage for Pulse
    targetCPUUtilizationPercentage: 60
    ## @param pulse.autoscaling.targetMemoryUtilizationPercentage Target memory utilisation percentage for Pulse
    targetMemoryUtilizationPercentage: 80

  ## @subsection Pulse Resources Configuration
  resources:
    # @param pulse.resources.limits Resource limits for Pulse
    limits:
      ## @param pulse.resources.limits.cpu CPU resource limits for Pulse
      cpu: 1000m
      ## @param pulse.resources.limits.memory Memory resource limits for Pulse
      memory: 1Gi
    # @param pulse.resources.requests Resource requests for Pulse
    requests:
      ## @param pulse.resources.requests.cpu CPU resource requests for Pulse
      cpu: 100m
      ## @param pulse.resources.requests.memory Memory resource requests for Pulse
      memory: 128Mi

  ## @subsection Pulse Service Configuration
  service:
    ## @param pulse.service.port Port of the Pulse service
    port: 8443
    ## @param pulse.service.metricsPort Port of the Pulse Metrics service
    metricsPort: 9090
    ## @param pulse.service.type Type of the Pulse service
    type: ClusterIP
    ## @param pulse.service.annotations Annotations for the Pulse service
    annotations: {}
    ## @param pulse.service.labels Labels for the Pulse service
    labels: {}

  ## @subsection Pulse Ingress Configuration
  ingress:
    ## @param pulse.ingress.enabled Specify if the Pulse Ingress is enabled
    enabled: true
    ## @param pulse.ingress.ingressClassName Ingress Class Name. May be required for k8s >= 1.18
    ingressClassName: "nginx"
    # @param pulse.ingress.annotations Annotations for ingress
    annotations:
      ## @param pulse.ingress.annotations.cert-manager.io/cluster-issuer Cluster issuer for ingress
      cert-manager.io/cluster-issuer: "letsencrypt-self-hosted"
      ## @param pulse.ingress.annotations.nginx.ingress.kubernetes.io/force-ssl-redirect Force SSL redirect for ingress
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # @param pulse.ingress.tls TLS configuration
    tls:
      ## @param pulse.ingress.tls[0].secretName TLS secret name for ingress
    - secretName: devzero-pulse-tls
        ## @param pulse.ingress.tls[0].hosts List of TLS hosts for ingress
      hosts:
      - "pulse.{{ .Values.domain }}"
    # @param pulse.ingress.hosts Hosts configuration
    hosts:
      ## @param pulse.ingress.hosts[0].host Host name for ingress
    - host: "pulse.{{ .Values.domain }}"
        # @param pulse.ingress.hosts[0].paths List of path configurations for ingress
      paths:
          ## @param pulse.ingress.hosts[0].paths[0].path Path for ingress
      - path: /
            ## @param pulse.ingress.hosts[0].paths[0].pathType Path type for ingress
        pathType: Prefix

## @section Buildqd Configuration
buildqd:
  ## @param buildqd.replicas Number of replicas for Buildqd
  replicas: 1

  ## @param buildqd.imageName Image name for Buildqd
  imageName: "buildqd"

  ## @param buildqd.schedulerName Scheduler name for Buildqd pods
  schedulerName: ""

  ## @param buildqd.priorityClassName Priority class name for Buildqd pods
  priorityClassName: ""

  ## @param buildqd.nodeSelector Node selector for Buildqd pods
  nodeSelector: {}

  ## @param buildqd.affinity Affinity settings for Buildqd pods
  affinity: {}

  ## @param buildqd.tolerations Tolerations for Buildqd pods
  tolerations: []

  ## @subsection Buildqd LogSrv Configuration
  logsrv:
    ## @param buildqd.logsrv.queue Queue URL for LogSrv
    queue: http://elasticmq:9324/queue/logsrv.fifo
    ## @param buildqd.logsrv.region Region for LogSrv
    region: elasticmq

  ## @subsection Buildqd Redis Configuration
  redis:
    ## @param buildqd.redis.url Redis URL for Buildqd
    url: redis://redis-headless:6379/0

  ## @subsection Buildqd Autoscaling Configuration
  autoscaling:
    ## @param buildqd.autoscaling.enabled Enable autoscaling for Buildqd
    enabled: false
    ## @param buildqd.autoscaling.minReplicas Minimum autoscaling replicas for Buildqd
    minReplicas: 1
    ## @param buildqd.autoscaling.maxReplicas Maximum autoscaling replicas for Buildqd
    maxReplicas: 3
    ## @param buildqd.autoscaling.targetCPUUtilizationPercentage Target CPU utilisation percentage for Buildqd
    targetCPUUtilizationPercentage: 60
    ## @param buildqd.autoscaling.targetMemoryUtilizationPercentage Target memory utilisation percentage for Buildqd
    targetMemoryUtilizationPercentage: 80

  ## @subsection Buildqd Resources Configuration
  resources:
    # @param buildqd.resources.limits Resource limits for Buildqd
    limits:
      ## @param buildqd.resources.limits.cpu CPU resource limits for Buildqd
      cpu: 1000m
      ## @param buildqd.resources.limits.memory Memory resource limits for Buildqd
      memory: 1Gi
    # @param buildqd.resources.requests Resource requests for Buildqd
    requests:
      ## @param buildqd.resources.requests.cpu CPU resource requests for Buildqd
      cpu: 100m
      ## @param buildqd.resources.requests.memory Memory resource requests for Buildqd
      memory: 128Mi

  ## @subsection Buildqd Service Configuration
  service:
    ## @param buildqd.service.port Port of the Buildqd service
    port: 8443
    ## @param buildqd.service.metricsPort Port of the Buildqd Metrics service
    metricsPort: 9090
    ## @param buildqd.service.type Type of the Buildqd service
    type: ClusterIP
    ## @param buildqd.service.annotations Annotations for the Buildqd service
    annotations: {}
    ## @param buildqd.service.labels Labels for the Buildqd service
    labels: {}

  ## @subsection Buildqd Buildkit Configuration
  buildkit:
    ## @subsection Buildkit Image Configuration
    image:
      ## @param buildqd.buildkit.image.repository Buildkit repository
      repository: docker.io/moby/buildkit
      ## @param buildqd.buildkit.image.pullPolicy Buildkit image policy
      pullPolicy: IfNotPresent
      ## @param buildqd.buildkit.image.tag Buildkit image tag
      tag: "v0.15.1"

    ## @subsection Buildkit Container Security Configuration
    securityContext:
      ## @param buildqd.buildkit.securityContext.privileged Privileged mode for Buildkit container
      privileged: true

    ## @param buildqd.buildkit.command Buildkit command
    command: buildkitd

    ## @param buildqd.buildkit.args Buildkit args
    args:
    - --addr
    - unix:///run/buildkit/buildkitd.sock
    - --addr
    - tcp://0.0.0.0:1234

    ## @subsection Buildkit Resources Configuration
    resources:
      # @param buildqd.buildkit.resources.limits Resource limits for Buildkit
      limits:
        ## @param buildqd.buildkit.resources.limits.cpu CPU resource limits for Buildkit
        cpu: 1000m
        ## @param buildqd.buildkit.resources.limits.memory Memory resource limits for Buildkit
        memory: 1Gi
      # @param buildqd.buildkit.resources.requests Resource requests for Buildkit
      requests:
        ## @param buildqd.buildkit.resources.requests.cpu CPU resource requests for Buildkit
        cpu: 100m
        ## @param buildqd.buildkit.resources.requests.memory Memory resource requests for Buildkit
        memory: 128Mi
    ## @subsection Buildkit Persistent Volume Configuration
    persistentVolumeClaim:
      ## @param buildqd.buildkit.persistentVolumeClaim.storageSize Storage size for Buildkit shared cache
      storageSize: 100Gi
## @section Web Configuration
web:
  ## @param web.replicas Number of replicas for Web
  replicas: 1
  ## @param web.imageName Image name for Web
  imageName: "web"
  ## @param web.schedulerName Scheduler name for Web pods
  schedulerName: ""
  ## @param web.priorityClassName Priority class name for Web pods
  priorityClassName: ""
  ## @param web.nodeSelector Node selector for Web pods
  nodeSelector: {}
  ## @param web.affinity Affinity settings for Web pods
  affinity: {}
  ## @param web.tolerations Tolerations for Web pods
  tolerations: []
  ## @param web.podLabels Pod labels for Web
  podLabels: {}
  ## @param web.podAnnotations Pod annotations for Web
  podAnnotations: {}
  ## @param web.annotations Annotations for Web
  annotations: {}
  ## @subsection Web Environment Configuration
  env:
    ## @param web.env.NEXT_PUBLIC_SELF_HOSTED Self-hosted mode for Next.js
    NEXT_PUBLIC_SELF_HOSTED: "true"
    ## @param web.env.NODE_TLS_REJECT_UNAUTHORIZED ignore SSL mode for Next.js
    NODE_TLS_REJECT_UNAUTHORIZED: "0"
    NEXTAUTH_URL: "https://{{ .Values.domain }}"
  ## @subsection Web Autoscaling Configuration
  autoscaling:
    ## @param web.autoscaling.enabled Enable autoscaling for Web
    enabled: false
    ## @param web.autoscaling.minReplicas Minimum autoscaling replicas for Web
    minReplicas: 1
    ## @param web.autoscaling.maxReplicas Maximum autoscaling replicas for Web
    maxReplicas: 3
    ## @param web.autoscaling.targetCPUUtilizationPercentage Target CPU utilisation percentage for Web
    targetCPUUtilizationPercentage: 60
    ## @param web.autoscaling.targetMemoryUtilizationPercentage Target memory utilisation percentage for Web
    targetMemoryUtilizationPercentage: 80

  ## @subsection Web Resources Configuration
  resources:
    # @param web.resources.limits Resource limits for Web
    limits:
      ## @param web.resources.limits.cpu CPU resource limits for Web
      cpu: 2000m
      ## @param web.resources.limits.memory Memory resource limits for Web
      memory: 4Gi
    # @param web.resources.requests Resource requests for Web
    requests:
      ## @param web.resources.requests.cpu CPU resource requests for Web
      cpu: 1000m
      ## @param web.resources.requests.memory Memory resource requests for Web
      memory: 2Gi

  ## @subsection Web Service Configuration
  service:
    ## @param web.service.port Port of the Web service
    port: 3000
    ## @param web.service.type Type of the Web service
    type: ClusterIP
    ## @param web.service.annotations Annotations for the Web service
    annotations: {}
    ## @param web.service.labels Labels for the Web service
    labels: {}

  ## @subsection Web Ingress Configuration
  ingress:
    ## @param web.ingress.enabled Specify if the Web Ingress is enabled
    enabled: true
    ## @param web.ingress.ingressClassName Ingress Class Name. May be required for k8s >= 1.18
    ingressClassName: "nginx"
    # @param web.ingress.annotations Annotations for ingress
    annotations:
      ## @param web.ingress.annotations.cert-manager.io/cluster-issuer Cluster issuer for ingress
      cert-manager.io/cluster-issuer: "letsencrypt-self-hosted"
      ## @param web.ingress.annotations.nginx.ingress.kubernetes.io/force-ssl-redirect Force SSL redirect for ingress
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # @param web.ingress.tls TLS configuration
    tls:
      ## @param web.ingress.tls[0].secretName TLS secret name for ingress
    - secretName: devzero-web-tls
        ## @param web.ingress.tls[0].hosts List of TLS hosts for ingress
      hosts:
      - "{{ .Values.domain }}"
    # @param web.ingress.hosts Hosts configuration
    hosts:
      ## @param web.ingress.hosts[0].host Host name for ingress
    - host: "{{ .Values.domain }}"
        # @param web.ingress.hosts[0].paths List of path configurations for ingress
      paths:
          ## @param web.ingress.hosts[0].paths[0].path Path for ingress
      - path: /
            ## @param web.ingress.hosts[0].paths[0].pathType Path type for ingress
        pathType: Prefix

## @section Vault Configuration
vault:
  ## @subsection Vault Job Configuration
  job:
    ## @param vault.job.enabled Enable Vault job
    enabled: true
    ## @param vault.job.address Vault address for job
    address: "vault:8200"
  ## @subsection Vault secrets engine Configuration
  secrets:
    ## @param vault.secrets.enabled Enable Vault job
    enabled: true
    ## @param vault.secrets.address Vault address for job
    address: "http://vault:8200"
    ## @param vault.secrets.path Vault address for job
    path: "devzero"
